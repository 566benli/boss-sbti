{
  "name": "boss-sbti-api",
  "private": true,
  "version": "0.1.0",
  "description": "老板 SBTI 后端 API（Cloudflare Worker + D1 + KV）",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init": "wrangler d1 execute boss-sbti --file=migrations/0001_init.sql",
    "d1:init:local": "wrangler d1 execute boss-sbti --local --file=migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  }
}
